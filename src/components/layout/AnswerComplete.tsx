import React from "react";

const AnswerComplete = () => {
  return (
    <span className=" bg-yellow text-orange text-xs px-2 py-1 rounded-md">
      답변 완료
    </span>
  );
};

export default AnswerComplete;
